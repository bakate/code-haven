import { EnrolledCoursesList } from "../components/enrolled-courses-list";

export const AllStudentEnrolledCoursesScreen = () => {
  return (
    <div>
      <EnrolledCoursesList />
    </div>
  );
};
